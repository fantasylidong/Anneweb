# Default settings for running Anneweb services with plain `docker run`
# You can override any value by exporting the variable before执行脚本,
# or修改本文件后再次运行脚本。

PROJECT_NAME=anneweb

# 镜像与构建
APP_IMAGE=anneweb/app:local
APP_HTTP_PORT=8080

# 数据库
MYSQL_ROOT_PASSWORD=secret
MYSQL_DATABASE=anneweb
MYSQL_USER=anneweb
MYSQL_PASSWORD=anneweb
SOURCEBANS_DATABASE=sourcebans
SOURCEBANS_USER=anneweb
SOURCEBANS_PASSWORD=anneweb
MYSQL_HOST_PORT=33060

# Redis & Mailpit 端口
REDIS_HOST_PORT=63790
MAILPIT_HTTP_PORT=8025
MAILPIT_SMTP_PORT=1025

# Laravel 应用常用环境变量
APP_ENV=local
APP_DEBUG=true
APP_URL=http://localhost:8080
LOG_CHANNEL=stack

# 队列设置
QUEUE_CONNECTION=redis
REDIS_HOST=anneweb-redis
